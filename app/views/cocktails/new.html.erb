<h1>Add a cocktail</h1>
<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name %>
  <%= f.submit "Add!", class: "btn btn-primary" %>
<% end %>

<%= link_to '<i class="fa fa-undo" aria-hidden="true"></i> Back'.html_safe, cocktails_path, class: "btn" %>
